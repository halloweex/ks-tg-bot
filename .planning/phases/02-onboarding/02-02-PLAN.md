---
phase: 02-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - ONBR-01
  - ONBR-02
  - ONBR-03
  - ONBR-04
  - ONBR-05
  - ONBR-06

must_haves:
  truths:
    - "Human verified: /start shows branded greeting with phone prompt for new user"
    - "Human verified: valid phone is accepted and looked up in APIs"
    - "Human verified: invalid phone format shows Ukrainian error"
    - "Human verified: returning user sees welcome back message without phone prompt"
    - "Human verified: user data persisted in SQLite (survives bot restart)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete onboarding flow — end-to-end test of /start, phone entry, API lookup, and returning user detection.

Purpose: Phase 2 is the critical gate for all downstream features. A verified phone flow ensures Phase 3 (menus, orders, support) can build on solid ground.

Output: Confirmed working onboarding or list of issues to fix.
</objective>

<execution_context>
@/Users/vladislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vladislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete onboarding flow</name>
  <files>bot/handlers/common.py, bot/handlers/onboarding.py</files>
  <action>
Human end-to-end verification of the onboarding flow built in 02-01.

What was built:
- /start command shows branded welcome with phone prompt (new users)
- Phone validation accepts +380XXXXXXXXX format, rejects invalid input
- Parallel Shopify + KeyCRM lookup finds orders by phone
- SQLite persists chat_id-to-phone mapping
- Returning users skip phone entry on subsequent /start

How to verify:
1. Start the bot: `cd /Users/vladislav/course-dbt/ks-tg-bot && python -m bot`
2. Open Telegram, find the bot (@koreanstory_bot or your test bot)

**Test A — New user flow:**
3. Send `/start` — expect branded Ukrainian greeting with phone prompt
4. Send `hello` — expect Ukrainian error about invalid phone format
5. Send `+380 67 123 4567` (with spaces) — should be accepted after normalization. If phone has orders in Shopify or KeyCRM, expect confirmation. If not found, expect error.
6. Send a real phone number that HAS orders in your Shopify/KeyCRM system in +380XXXXXXXXX format — expect confirmation

**Test B — Returning user flow:**
7. After successful phone verification, send `/start` again — expect welcome back message (NOT the phone prompt)
8. Stop the bot (Ctrl+C), restart it (`python -m bot`), send `/start` — expect welcome back again (data persisted in SQLite)

**Test C — Verify SQLite:**
9. Check database: `sqlite3 bot_data.db "SELECT * FROM users;"` — expect a row with your chat_id and phone

Resume signal: Type "approved" if all tests pass, or describe which tests failed.
  </action>
  <verify>
    <automated>cd /Users/vladislav/course-dbt/ks-tg-bot && sqlite3 bot_data.db "SELECT count(*) FROM users;" 2>/dev/null || echo "No users yet — run manual test first"</automated>
  </verify>
  <done>Human confirmed: new user onboarding flow works end-to-end; returning user detection works; SQLite persistence verified across bot restart</done>
</task>

</tasks>

<verification>
Human confirms all test scenarios (A, B, C) pass successfully.
</verification>

<success_criteria>
- New user sees branded greeting and can enter phone number
- Invalid phone formats are rejected with Ukrainian error message
- Valid phone triggers API lookup and proceeds (or shows "not found" if no orders)
- Returning user skips phone entry entirely
- Data survives bot restart (SQLite persistence verified)
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding/02-02-SUMMARY.md`
</output>
